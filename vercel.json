{
  "buildCommand": "pnpm build --filter=web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --no-frozen-lockfile --filter=@school-management/web --ignore-scripts",
  "framework": null
}
